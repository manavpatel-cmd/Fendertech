/**
 * TypeScript resolves this file for `tsc` / editors. Metro still bundles
 * `createDefaultTransport.native.ts` or `.web.ts` when those exist.
 */
export { createDefaultTransport } from "./createDefaultTransport.native";
